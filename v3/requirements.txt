# Python dependencies for the Pythos audio worker (wake word, STT, TTS, tools).
#
# The AI brain does NOT run in Python. It runs locally via Ollama + Gemma 4,
# which the Electron main process calls over HTTP. Ollama is a SYSTEM dependency,
# not a pip package:
#   1. Install Ollama:  https://ollama.com/download  (macOS: brew install ollama)
#   2. Pull the model:  ollama pull gemma4:12b   (see scripts/install-ollama-models.sh)
#
# PyAudio also needs the native PortAudio library:
#   macOS:         brew install portaudio
#   Debian/Ubuntu: sudo apt-get install -y portaudio19-dev
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
colorama==0.4.6
flatbuffers==25.12.19
idna==3.16
iniconfig==2.3.0
joblib==1.5.3
numpy==2.4.6
onnxruntime==1.26.0
packaging==26.2
pluggy==1.6.0
protobuf==7.35.0
PyAudio==0.2.14
pycparser==3.0
pygame==2.6.1
Pygments==2.20.0
pytest==9.0.3
requests==2.34.2
scikit-learn==1.8.0
scipy==1.17.1
srt==3.5.3
threadpoolctl==3.6.0
tqdm==4.67.3
urllib3==2.7.0
vosk==0.3.44
websockets==16.0
